from setuptools import find_packages, setup

setup(
    name='{{packageName}}',
    version='{{packageVersion}}',
    description='TeamCity REST API Client',
    packages=find_packages(),
    install_requires = [
        'requests'
    ]
)
